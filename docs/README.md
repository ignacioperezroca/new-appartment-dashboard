# New Appartment Dashboard project notes

## Purpose
Preserve the dashboard app in a maintainable source-first repository structure.

## Structure
- Keep app logic in `src/`.
- Keep the entry page in `index.html`.
- Keep maintenance notes and inventory in `docs/`.

## Maintenance
- Do not replace the app with a generic summary page.
- Keep build and deployment settings aligned with the Vite app.
